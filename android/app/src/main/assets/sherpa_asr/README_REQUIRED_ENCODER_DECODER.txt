Required for the current Whisper-style Sherpa ASR config:
- config.json
- tokens.txt
- encoder.onnx
- decoder.onnx

The final package includes config.json and tokens.txt copied from the uploaded files.
The large encoder.onnx and decoder.onnx files were not uploaded here, so copy them into this folder before building the APK.
