diff --git a/.github/workflows/nova-tecno-arm64-apk.yml b/.github/workflows/nova-tecno-arm64-apk.yml
index 2934b613247df8d6c9b65dd73515a417373107a2..80b24a488960fda19362df5c588301d32efab26b 100644
--- a/.github/workflows/nova-tecno-arm64-apk.yml
+++ b/.github/workflows/nova-tecno-arm64-apk.yml
@@ -70,6 +70,7 @@ jobs:
         run: >-
           flutter test
           test/runtime/nova_runtime_integrity_contract_test.dart
+          test/runtime/nova_voice_authority_fail_closed_contract_test.dart
           test/runtime/nova_verified_device_action_contract_test.dart
           test/runtime/nova_contact_call_target_resolver_test.dart
 
diff --git a/.gitignore b/.gitignore
index 0b851aaad377060433b4771e7379623cdcda553e..ca15512524ac78d3493cd86c2e886f3cdb1b8502 100644
--- a/.gitignore
+++ b/.gitignore
@@ -56,3 +56,7 @@ android/app/src/main/assets/nova/models/whisper/
 android/app/src/main/assets/nova/models/voice_id/
 android/app/src/main/assets/nova/models/vad/
 android/app/src/main/assets/nova/models/tts/
+
+# One-time verified patch staging must never survive in source control.
+/tooling/.nova_patch_apply/
+/tooling/.nova_refactor_bundle/
diff --git a/lib/services/identity/voice_authorization_runtime_service.dart b/lib/services/identity/voice_authorization_runtime_service.dart
index 0266fb69cee0a6e66246af7219746702b4e434d0..989d3b0fba5ba1ed33f6d50c10c9046f4eb0bbbe 100644
--- a/lib/services/identity/voice_authorization_runtime_service.dart
+++ b/lib/services/identity/voice_authorization_runtime_service.dart
@@ -386,7 +386,7 @@ class VoiceAuthorizationRuntimeService {
     Duration trustedDailyWindow = const Duration(hours: 24),
     Duration trustedRecentWindow = const Duration(hours: 20),
     Duration conversationWindow = const Duration(hours: 8),
-    bool allowContinuityReuse = true,
+    bool allowContinuityReuse = false,
     String preferredVoiceId = '',
   }) async {
     if (allowContinuityReuse) {
@@ -414,7 +414,7 @@ class VoiceAuthorizationRuntimeService {
     int maxDurationSeconds = 4,
     String outputName = 'nova_runtime_auth',
     double minSimilarity = 0.64,
-    bool allowContinuityReuse = true,
+    bool allowContinuityReuse = false,
     String preferredVoiceId = '',
   }) async {
     final inspection = await inspectPreferContinuityThenFresh(
@@ -422,9 +422,15 @@ class VoiceAuthorizationRuntimeService {
       maxDurationSeconds: maxDurationSeconds,
       outputName: outputName,
       minSimilarity: minSimilarity,
-      allowContinuityReuse: allowContinuityReuse,
+      allowContinuityReuse: false,
       preferredVoiceId: preferredVoiceId,
     );
+    if (!inspection.captureSucceeded) {
+      return const VoiceAccessDecision(
+        level: VoiceAccessLevel.denied,
+        message: 'Güncel ses doğrulaması olmadan yetki verilemez.',
+      );
+    }
     return inspection.decision;
   }
 
@@ -439,6 +445,7 @@ class VoiceAuthorizationRuntimeService {
       maxDurationSeconds: maxDurationSeconds,
       outputName: outputName,
       minSimilarity: minSimilarity,
+      allowContinuityReuse: false,
     );
 
     return decision.level == VoiceAccessLevel.owner ||
@@ -455,6 +462,7 @@ class VoiceAuthorizationRuntimeService {
       maxDurationSeconds: maxDurationSeconds,
       outputName: outputName,
       minSimilarity: minSimilarity,
+      allowContinuityReuse: false,
     );
 
     return decision.level == VoiceAccessLevel.owner ||
diff --git a/lib/services/runtime/nova_hotpath_owner_service.dart b/lib/services/runtime/nova_hotpath_owner_service.dart
index dc8d2444ba70dfa2373662188a0b9a06acdf69b2..7e69b4d387e1cde5bd37dc7215cbd9dc6e02b0c4 100644
--- a/lib/services/runtime/nova_hotpath_owner_service.dart
+++ b/lib/services/runtime/nova_hotpath_owner_service.dart
@@ -24,11 +24,11 @@ class NovaHotpathOwnerResult {
   const NovaHotpathOwnerResult({
     required this.handledByRuntime,
     required this.handledByAi,
-    String spokenText = '',
+    this.spokenText = '',
     this.aiResponse,
     this.runtimeResult,
     this.actionSummary = '',
-  }) : spokenText = '';
+  });
 }
 
 class NovaHotpathOwnerService {
@@ -236,7 +236,7 @@ class NovaHotpathOwnerService {
       return NovaHotpathOwnerResult(
         handledByRuntime: true,
         handledByAi: true,
-        spokenText: '',
+        spokenText: spoken,
         aiResponse: finalAiResponse,
         runtimeResult: runtimeResult,
         actionSummary: actionSummary,
@@ -247,7 +247,7 @@ class NovaHotpathOwnerService {
     return NovaHotpathOwnerResult(
       handledByRuntime: false,
       handledByAi: true,
-      spokenText: '',
+      spokenText: spoken,
       aiResponse: aiResponse,
       runtimeResult: runtimeResult,
       actionSummary: actionSummary,
diff --git a/lib/services/system/nova_continuous_listening_runtime_service.dart b/lib/services/system/nova_continuous_listening_runtime_service.dart
index f778b0f5b8639b51e064efff9d0d955068e494f2..3ade15b694752ab2aef9cff3b81f7ca8f5ed1d7f 100644
--- a/lib/services/system/nova_continuous_listening_runtime_service.dart
+++ b/lib/services/system/nova_continuous_listening_runtime_service.dart
@@ -45,8 +45,8 @@ class NovaContinuousListeningRuntimeService {
   final NovaPlaybackEchoFilterService playbackEchoFilterService;
   final NovaRecentSpeakerService recentSpeakerService;
   final NovaRoomConversationContextService roomConversationContextService;
-  final bool Function()? isWakeWordEnabled;
-  final bool Function()? isCallHandlingEnabled;
+  final bool Function() isWakeWordEnabled;
+  final bool Function() isCallHandlingEnabled;
   final String wakeWord;
   final List<String> wakePhraseAliases;
   final Future<bool> Function(String prompt) onAuthorizedPrompt;
@@ -109,8 +109,8 @@ class NovaContinuousListeningRuntimeService {
     this.playbackEchoFilterService =
         const NovaPlaybackEchoFilterService(),
     this.recentSpeakerService = const NovaRecentSpeakerService(),
-    this.isWakeWordEnabled,
-    this.isCallHandlingEnabled,
+    required this.isWakeWordEnabled,
+    required this.isCallHandlingEnabled,
     required this.wakeWord,
     this.wakePhraseAliases = const <String>[],
     required this.onAuthorizedPrompt,
@@ -381,8 +381,7 @@ class NovaContinuousListeningRuntimeService {
         final bool sleeping = lifecycleService.isSleeping;
         if (powerService.isFullyShutdown) {
           final bool shutdownCallHandlingEnabled =
-              isCallHandlingEnabled?.call() ?? true;
+              isCallHandlingEnabled();
           if (shutdownCallHandlingEnabled &&
               callCompanionRuntimeService != null &&
               callStateService != null) {
@@ -445,7 +444,7 @@ class NovaContinuousListeningRuntimeService {
             }
           }
 
-          final bool wakeWordEnabled = isWakeWordEnabled?.call() ?? true;
+          final bool wakeWordEnabled = isWakeWordEnabled();
           if (!wakeWordEnabled) {
             await Future<void>.delayed(const Duration(milliseconds: 1800));
             continue;
@@ -458,13 +457,44 @@ class NovaContinuousListeningRuntimeService {
             continue;
           }
 
+          final wakeInspection = await authorizationRuntimeService
+              .inspectFreshExternalSample(
+                maxDurationSeconds: 3,
+                outputName: 'nova_shutdown_wake_auth',
+                minSimilarity: 0.62,
+              );
+          final wakeDecision = wakeInspection.decision;
+          final wakeAuthorized =
+              wakeInspection.captureSucceeded &&
+              (wakeDecision.level == VoiceAccessLevel.owner ||
+                  wakeDecision.level == VoiceAccessLevel.authorizedGuest);
+          if (!wakeAuthorized) {
+            if (onUnauthorizedOrStatus != null &&
+                !wakeDecision.suppressStatusBroadcast) {
+              await _emitStatusIfChanged(
+                wakeDecision.message.trim().isEmpty
+                    ? 'Tam kapalı moddan uyanmak için güncel ses doğrulaması gerekiyor.'
+                    : wakeDecision.message.trim(),
+                onUnauthorizedOrStatus,
+              );
+            }
+            await Future<void>.delayed(const Duration(milliseconds: 1200));
+            continue;
+          }
+
           lifecycleService.wake();
           await powerService.setFullyOn(userInitiated: true);
           _softSpeakerIdentityUntil = DateTime.now().add(
-            const Duration(hours: 2),
+            const Duration(minutes: 20),
           );
           _authorizedConversationUntil = DateTime.now().add(
-            const Duration(hours: 2),
+            const Duration(minutes: 20),
           );
           _conversationHoldUntil = DateTime.now().add(
             const Duration(minutes: 20),
